---
title: "Interplay of Text, Reader And Context (InTRACt) in children’s digital reading comprehension: Preregistration"
format:
  pdf:
    documentclass: article
    geometry:
      - margin=2.54cm
    fontsize: 12pt
    linestretch: 2
    mainfont: "Times New Roman"
    number-sections: true
    toc: true
    link-citations: true
    include-in-header:
      text: |
        \usepackage{caption}
        \captionsetup[table]{labelfont=bf,textfont=it,labelsep=newline,justification=raggedright,singlelinecheck=false,skip=4pt}
        \captionsetup[figure]{labelfont=bf,textfont=it,labelsep=newline,justification=raggedright,singlelinecheck=false,skip=4pt}
        \usepackage{titlesec}
        \titlespacing*{\section}{0pt}{1.2ex plus 0.5ex minus 0.2ex}{0.8ex plus 0.2ex}
        \titlespacing*{\subsection}{0pt}{1.0ex plus 0.4ex minus 0.2ex}{0.6ex plus 0.2ex}
        \titlespacing*{\subsubsection}{0pt}{0.8ex plus 0.3ex minus 0.1ex}{0.4ex plus 0.1ex}
        \usepackage{booktabs}
        \setlength{\aboverulesep}{4pt}
        \setlength{\belowrulesep}{4pt}
        \renewcommand{\arraystretch}{1.6}
        \usepackage{tocloft}
        \setlength{\cftbeforetoctitleskip}{-20pt}
  html:
    toc: true
    number-sections: true
    embed-resources: true
fig-cap-location: top
tbl-cap-location: top
fig-pos: "H"
bibliography: "InTRACt references.bib"
csl: apa7.csl
---

# Abstract {.unnumbered}

Children's reading comprehension involves the interplay of three dimensions: the text (its lexical, syntactic, and discourse characteristics), the reader (developing reading fluency, built through accumulated reading experience), and the reading context (the digital medium and the child's linguistic environment). Despite substantial laboratory-based progress in characterising each dimension in isolation, their joint influence under naturalistic, multilingual conditions is poorly understood. The present preregistration describes the InTRACt study, which addresses this gap by analysing large-scale behavioural logs from *Let's Story*, an AI-powered storytelling feature of the *Applaydu* edutainment app deployed across 19 languages. Reading time per word is the outcome. Because the platform records no cross-linguistically comparable comprehension measure, the confirmatory inferences concern reading behaviour and its predictors rather than comprehension itself. For each story, a broad range of computational text features is reduced to principal components before model entry. Within-child reading experience, defined as how much the child has read across prior sessions, is treated as a developing-fluency predictor and is expected to speed reading as it accumulates. All confirmatory analyses use Bayesian multilevel models with random effects for child and language, so that text-feature effects are estimated across the 19 languages of the corpus. Two hypotheses are pre-registered: that syntactic and lexical complexity slow reading while discourse coherence accelerates it, and that children read progressively faster as reading experience accumulates.

{{< pagebreak >}}

# Background

## Text-Level Predictors of Reading Comprehension

A longstanding tradition in computational psycholinguistics explains reading comprehension in terms of measurable text and reader variables. This body of work identifies specific linguistic features, operating at the lexical, syntactic and discourse levels, that predict how readers engage with and understand text.

At the lexical level, word frequency is among the most reliable predictors of reading behaviour. High-frequency words are recognised faster and fixated for shorter durations than low-frequency words, a pattern replicated across dozens of eye-tracking studies and remarkably consistent across languages and orthographies. Word length and frequency exert effects of strikingly similar magnitude across twelve typologically diverse alphabetic languages [@kupermanWordLength2024], showing the cross-linguistic regularity on which a multilingual analysis like the present one depends. That regularity of feature effects coexists, however, with large between-language differences in how quickly literacy is acquired and how fast text is read: foundation literacy develops markedly more slowly in deep orthographies such as English than in shallow ones [@seymourFoundationLiteracyAcquisition2003], grain-size accounts predict systematic cross-orthography differences in reading strategy [@zieglerReadingAcquisitionDevelopmental2005], and multilingual eye-tracking documents wide between-language variation in reading speed [@siegelmanExpandingHorizonsCrosslinguistic2022]. The present design absorbs these differences through within-language standardisation and language-level random effects (see Analytic Plan). In children, lexical access is less automatised, so lexical properties such as frequency and semantic diversity exert strong and still-developing effects on word reading [@hsiaoSemanticDiversityFrequency2018], and eye-movement studies of connected reading show that children's fixation times are disproportionately lengthened by low-frequency and long words relative to adults' [@josephUsingEyeMovements2013; @tiffinrichardsWordLengthFrequency2015]. Frequency norms derived from subtitle corpora (e.g., the Subtitle Lexical Frequencies [SUBTLEX] databases), including SUBTLEX-UK for British English [@vanheuvenSubtlexUKNewImproved2014], have become standard tools for operationalising this variable, offering estimates that closely approximate everyday language exposure [@brysbaertMovingKuceraFrancis2009]. More recently, resources such as the Children and Young People's Books Lexicon (CYP-LEX), derived from 1,200 books read by children and young people in the United Kingdom, offer a more ecologically valid frequency baseline for studies of developing readers, capturing the vocabulary children actually encounter in their reading environment [@korochkinaChildrenYoungPeoples2024]. A related lexical property, word concreteness, also shapes comprehension: concrete, highly imageable words (e.g., *apple*, *river*) are processed more quickly than abstract words (e.g., *justice*, *theory*) [@magnaboscoEyeSemanticsStudy2024]. This concreteness advantage is classically attributed to dual-coding theory, in which concrete words activate both verbal and imagistic representations [@paivioDualCodingTheory1991], and in contemporary work to grounded and hybrid modal–amodal accounts [@bernabeuLanguageSensorimotorSimulation2022; @kaupInterplayModalAmodal2025]. It is expected to matter especially early in development: earlier-acquired word meanings are systematically more concrete, consistent with sensorimotor grounding of the early lexicon [@murakiDevelopmentAbstractWord2025]. Consistent with this, @pickrenContributionTextCharacteristics2022 demonstrated that text characteristics including word emotionality and concreteness predict significant unique variance in reading performance among elementary school children, above and beyond traditional readability metrics. A third lexical variable, the diversity of vocabulary within a text, contributes to text difficulty. It is operationalised through indices such as the type/token ratio (TTR) or the Measure of Textual Lexical Diversity (MTLD) [@mccarthyMtldVocdHdd2010]. Because vocabulary knowledge is itself a strong determinant of children's reading comprehension [@lervagVocabularyKnowledgeCritical2010], the demands of lexically varied text are expected to fall most heavily on readers with weaker vocabularies, including many children.

At the syntactic level, sentence structure exerts its own demands on the reader. Measures such as mean dependency distance (the average linear distance between syntactically related words), the number of embedded clauses and subordination ratios capture aspects of structural complexity, and sensitivity to such structure is a distinct contributor to reading comprehension [@nielsenSyntacticComprehensionSeparate2025]. Longer dependency distances, in particular, impose greater working memory demands, as readers must maintain information across intervening material before resolving a syntactic relationship [@gibsonLinguisticComplexityLocality1998]. This load weighs especially heavily on children, whose syntactic processing is still developing [@delageSyntaxWorkingMemory2019]. Converging evidence comes from @marjokorpiGrammaticalUnderstandingPredicts2025, who found that explicit grammatical understanding was a robust predictor of reading comprehension in secondary-school students even after controlling for socioeconomic background, writing ability, and other literacy-related factors, indicating that sensitivity to syntactic structure continues to underlie comprehension development well into adolescence.

Beyond words and sentences, the coherence of a text, the degree to which successive ideas are logically and semantically connected, is central to comprehension. Computational measures of coherence, including latent semantic analysis (LSA)-based sentence-to-sentence similarity, co-reference overlap and connective density, index the ease with which readers can integrate information across a passage [@crossleyToolAutomaticAnalysis2019]. On construction–integration accounts, breaks in coherence oblige the reader to generate bridging inferences, a process that costs time and working memory [@kintschRoleKnowledgeDiscourse1988]. Highly coherent texts accordingly support faster integration and better comprehension, particularly for readers with lower prior knowledge [@mcnamaraAreGoodTexts1996], which is the relevant case for child readers encountering newly generated stories. Complementing these deeper semantic relations are surface-level cohesion signals: explicit connectives such as *because*, *however* and *therefore* that mark the logical structure of a text. Automated tools such as Coh-Metrix [@graesserCohmetrixMeasuresText2014] now make it possible to compute many coherence and cohesion indices at once, enabling large-scale analyses of text difficulty that would be impractical to conduct by hand [@crossleyDevelopingLinguisticConstructs2025].

These individual variables have also been combined into composite indices of text difficulty. Classical readability formulae (e.g., Flesch–Kincaid, Dale–Chall) aggregate surface features such as word length and sentence length into single scores. Although limited in linguistic sophistication, they remain widely used in educational and applied contexts [@crossleyAssessingReadabilityFormulas2023]. More recent multi-dimensional frameworks integrate frequency, concreteness, syntactic complexity and coherence into richer characterisations of text difficulty, moving beyond simple heuristics towards discourse-level representations [@crossleyAssessingReadabilityFormulas2023; @liExploringSyntacticComplexity2025; @crossleyDevelopingLinguisticConstructs2025].

Taken together, this body of research establishes that reading comprehension is jointly shaped by many text-level variables operating across multiple linguistic levels, alongside substantial reader differences in vocabulary and language skill [@nationChildrensReadingDifficulties2019; @duffEstimatesIndividualDifferences2020] — one facet of a broader reconceptualisation of reading comprehension as a multidimensional process shaped by reader, purpose and context [@goldmanReadingComplexImplications2024]. These variables predict reading behaviour — including reading time, fixation patterns and comprehension accuracy — in both children and adults, though with developmental differences in magnitude and direction. The present study builds on this tradition by incorporating these established predictors as fixed effects in our analytic models (see Analytic Plan). We turn next to a more recent line of work that complements these feature-based approaches with neural language models.

## Computational Modelling of Reading With Neural Language Models

While the text-level features reviewed above offer interpretable, theory-driven predictors of reading behaviour, a parallel line of research has begun to ask whether the representations learned by neural language models can capture the same reading phenomena, and potentially go beyond what hand-crafted features achieve. This approach treats the internal states of large language models (LLMs) as proxies for the representations that the human language system constructs during reading, and evaluates their fit to behavioural and neural data.

In adults, language models predict reading behaviour with considerable accuracy: their word-by-word predictions reproduce eye-movement patterns across different reading tasks [@hahnModelingTaskEffects2023] and align closely with neural responses to sentences [@hosseiniArtificialNeuralNetwork2024], indicating that core linguistic mechanisms, chiefly word predictability, are well captured by current models.

Extending this work to child readers presents both opportunities and challenges. @monaghanLiteracyEarlyLanguage2023 reviews how computational models originally developed for adult word reading have been adapted to simulate literacy acquisition in children, tracing the developing connections between orthography, phonology and semantics across languages. Empirical work has begun to complement these simulations with large-scale behavioural data: @sortkaerBothSidesStory2021, for instance, analysed logs from approximately 11,000 Danish schoolchildren using a reading app (BookBites) and found a moderate positive correlation (*r* ≈ 0.4) between app-derived reading speed and standardised reading test scores. This result indicates that computational metrics extracted from naturalistic digital reading can meaningfully index developing literacy.

In sum, neural language models complement the traditional text-feature approach: where hand-crafted variables capture well-understood sources of variation, model-derived measures may capture subtler distributional and contextual regularities [@seidenbergImpactLanguageExperience2018]. However, for child readers, the evidence base is thinner, and many of the traditional text-level variables (frequency, coherence, syntactic complexity) may still be the most informative predictors. Our study is positioned at this intersection, using both established linguistic features and a model-derived index of word predictability (surprisal) to predict children's reading behaviour in a large multilingual sample. @tbl-adults-children summarises the key studies from both traditions that inform our hypotheses.

```{=latex}
\begingroup\setstretch{1.0}\small
```

| **Study** | **Readers** | **Method and key findings** |
|---|---|---|
| @hahnModelingTaskEffects2023 | Adults (*N* ≈ 20, eye-tracking) | NEAT model (recurrent neural network [RNN] with attention) vs. human reading tasks. Predicted fixation patterns across tasks; task-specific fixation differences accounted for by optimal attention allocation. |
| @hosseiniArtificialNeuralNetwork2024 | Adults (*N* ≈ 20, fMRI) | GPT-2 LLM (100M words) vs. brain responses. Near-maximal alignment with cortical responses after ~100M words of training, with only marginal gains thereafter. |
| @monaghanLiteracyEarlyLanguage2023 | Children (review) | Narrative review of computational literacy models. Adult word-reading models extend to child literacy; vocabulary growth and reading exposure interact across languages. |

: Representative Computational-Reading Studies by Age Group {#tbl-adults-children tbl-colwidths="[22,18,60]"}

*Note.* NEAT = Neural Attention Trade-off model; RNN = recurrent neural network; LLM = large language model; fMRI = functional magnetic resonance imaging. Sample sizes are as reported in the source publications.

```{=latex}
\endgroup
```

## Reading Experience and the Growth of Reading Fluency

The reader does not stay fixed across the period of observation: children grow more fluent as they read more. The cognitive mechanism is well established. Under automaticity theory, practice renders word recognition increasingly automatic, so that it demands less attention and proceeds faster, freeing resources for comprehension [@labergeTowardTheory1974]. On the complementary self-teaching account, each successful decoding of a novel word lays down orthographic representations that speed its subsequent recognition [@sharePhonologicalRecodingSelfteaching1995], and the resulting gains in word-level efficiency free capacity for text-level processing [@perfettiReadingAbilityLexical2007]. This mechanism is directly engaged by an app in which every story is newly generated. At the level of individual differences, the volume of reading a child accumulates is reliably associated with the growth of reading skill: a meta-analysis of print exposure from infancy to early adulthood found that how much children read predicts decoding fluency, comprehension and vocabulary, with the association strengthening across development [@molToReadOr2011]. These relations are widely held to be reciprocal, in that more skilled readers read more, and more reading further builds skill. This reciprocity is the "Matthew effect" by which early advantages compound over time [@stanovichMatthewEffectsReading1986].

The within-child direction of this relation is, however, not settled for younger readers. A longitudinal study following children from age 5 to 15 found reciprocal links between print exposure and reading skill throughout, but the effect of accumulated practice on later reading emerged only in adolescence, with skill driving amount more than the reverse during the early school years [@vanbergenHowArePractice2021]. Large-scale panel data also indicate that, among schoolchildren, the amount of time spent reading predicts subsequent growth in reading comprehension, and that the concurrent association weakens from the school years into adulthood [@locherRelationTimeSpent2020]. Other longitudinal work documents substantial between-child variation in how quickly fluency develops [@khanolainenPredictingReading2024], and shorter-term evidence indicates that readers who read more text gain reading speed faster than those who read less [@schmidtkeReadingExperience2024]. Such effects are increasingly measurable from naturalistic behavioural logs: in primary-school children, app-logged reading time predicts reading fluency more strongly than parental self-report and retains predictive value when both are entered together [@brossetteMeasuringReading2026]. This mixed but suggestive picture motivates testing, within the same children and at scale, whether reading time per word falls as a child accumulates reading experience across sessions (H2). The naturalistic design is well placed to address this question, which we frame as associational rather than developmental given the short observation window.

## App-Based Data in Reading Research

Digital reading is now widespread and much studied [@altamuraHowDigitalReading2025; @furenesComparisonChildrensReading2021], and reading on screen carries a small but reliable comprehension cost relative to paper [@delgadoDontThrowAway2018], so findings from a screen-based corpus characterise digital reading specifically. Yet few computational studies have used app-generated logs as a window into reading processes. The work that does exist falls broadly into two categories, which differ in their origins and scientific utility.

The first category comprises research and educational apps — platforms designed with learning objectives and, often, data collection in mind. @sortkaerBothSidesStory2021 exemplifies this approach: by analysing reading logs from BookBites, a classroom-deployed app, they showed that passively recorded reading speed correlated meaningfully with standardised reading ability. Such studies establish the principle that app logs can reflect underlying cognitive processes, but they typically operate within controlled educational settings and with relatively constrained populations.

The second category, commercial 'edutainment' apps, is far less explored in the academic literature, despite its vastly larger user base. These apps combine entertainment with incidental learning goals, and their data are generated under naturalistic, unconstrained conditions. A case in point is *Let's Story*, an artificial intelligence (AI)-powered digital storytelling feature embedded within *Applaydu*, an edutainment app developed by Gameloft and Ferrero International. The feature enables families to co-create storybooks using Microsoft's generative AI tools across 19 languages [@ratnerDidRobotWrite2025], and the platform itself passively logs each session's reading time (see Materials). Children select from a menu of story elements (plot, setting, character and theme), and the app generates a unique narrative that is then read aloud by an AI narrator in the family's chosen language. To our knowledge, no previous study has yet analysed data from Applaydu or any comparable commercial edutainment platform at scale through the lens of computational psycholinguistics. The existing literature on reading apps instead consists primarily of reviews and small-scale experiments examining specific app features. Content analyses have repeatedly found commercial educational apps to vary widely in instructional quality [@furlongFindingNeedleHaystack2025; @chatzopoulosEvaluationGooglePlay2023], a caveat that applies to the Applaydu platform examined here.

A further consideration is that the stories children read in *Let's Story* are written not by human authors but by a generative language model [@ratnerDidRobotWrite2025], which raises the question of how representative such text is of natural language. The case for treating it as broadly representative is substantial. Modern language models produce fluent, grammatical text and demonstrably capture non-trivial aspects of linguistic structure, to the point of serving as working model systems for usage-based theories of language [@futrellHowLinguisticsLearned2026]. Moreover, model-derived word predictability predicts human reading behaviour across languages (see Computational Modelling of Reading With Neural Language Models). That case is contested on both theoretical and empirical grounds, however. Critics argue that language models have not demonstrated mastery of formal syntactic structure [@murphyMachineYearningLLMs2026], and quantitative comparisons find measurable differences between model-generated and human-written text: less varied vocabulary, distinct distributions of dependency and constituent types, less optimised dependency distances, and a more positive, less negative emotional register [@munozortizContrastingLinguisticPatterns2024]. These differences fall squarely on the lexical-diversity, syntactic and affective dimensions measured in this study. For children's stories specifically, generated vocabulary tends to exceed age-appropriate levels [@valentiniAutomaticGenerationSimplification2023]. Three design decisions follow. First, all text features are computed directly on each story as served to the child, so the analyses assume nothing about the corpus's resemblance to human-authored children's literature. Second, the distribution of every feature will be reported, making transparent the difficulty range actually sampled and any shift or restriction relative to published children's-book norms (see Materials). Third, the conclusions are scoped accordingly: they concern children's reading of AI-generated stories (an increasingly common reading environment in its own right), and generalisation to human-authored text is treated as an open empirical question rather than assumed.

This gap between the commercial availability of reading-app data and its scientific use is notable. @tbl-app-data summarises the small number of relevant studies, along with their sample contexts and key findings.

```{=latex}
\begingroup\setstretch{1.0}\small
```

| **Study** | **App type and sample** | **Data, focus, and key findings** |
|---|---|---|
| @bootonImpactMobileApplication2023 | Educational e-books; children 3–11 yrs (11 studies) | App features: narration, augmented reality (AR), prompts, hotspots. Narration improved comprehension for pre-readers; AR supported language learning; hotspots showed no benefit. |
| @chatzopoulosEvaluationGooglePlay2023 | General educational, Google Play; *N* = 50 apps | Rubric-based quality appraisal. Most apps scored above-to-average overall, though a third fell below the scale midpoint on educational content; app-store ratings diverged from expert scores for individual apps. |
| @chuangSystematicReviewEffectiveness2023 | Edutainment/educational; 3–8 yr olds (50 studies) | Interactive book features. Story-congruent multimedia enhancements generally supported comprehension and vocabulary; unrelated game features distracted attention and harmed reading performance. |
| @furlongFindingNeedleHaystack2025 | Commercial phonics apps; *N* = 309 | Content analysis of app quality. Majority lacked structured pedagogy; only ~27% met expert criteria; consumer ratings uncorrelated with expert evaluations. |
| @sortkaerBothSidesStory2021 | Educational (BookBites); children *N* ≈ 10,900, grades 0–9 (correlations for grades 3–7) | App reading time vs. standardised test scores. Reading speed correlated with ability (*r* = .39–.48); models predicted 16–18% of variance in test performance. |

: Selected Studies on App-Based Reading Data {#tbl-app-data tbl-colwidths="[22,23,55]"}

*Note.* AR = augmented reality. Sample sizes, correlations, and variance estimates are as reported in the source publications.

```{=latex}
\endgroup
```

The present project addresses this gap directly. By analysing large-scale multilingual behavioural logs from the *Let's Story* feature of Applaydu, we aim to test whether the text-level variables identified in the laboratory-based literature (frequency, lexical diversity, syntactic complexity, coherence) predict children's reading behaviour in the wild, across the 19 languages of the corpus.

These aims translate into two research questions that the confirmatory analyses address directly. The first (RQ1) asks whether the lexical-distributional, syntactic-structural, and discourse-coherence properties of a story predict how long children spend reading each word. The second (RQ2) concerns whether children read progressively faster as they accumulate reading experience across successive sessions within the app. Each question maps onto one of the two directional hypotheses set out below, which the study design table summarises together with their statistical tests and the interpretation of possible outcomes (@tbl-design).

# Methods

## Participants

The study sample consists of data from the *Let's Story* feature of *Applaydu*, a commercially deployed edutainment app developed by Gameloft and Ferrero International that offers a range of activities targeting different skills. *Let's Story* is one such activity, enabling families to co-create AI-generated storybooks using Microsoft's generative AI tools across 19 languages. In the first six months following its launch in late 2024, *Let's Story* facilitated over 2.4 million story sessions and generated more than 40,000 English-language stories [@ratnerDidRobotWrite2025]. We will include all children (aged 7 years and older) who meet the inclusion criteria below [exact *N* to be determined after data extraction]. All data are anonymised and collected passively, with no additional experimental manipulation. Children will be included if (a) parental consent was registered in-app, (b) they were aged 7 years or older as reported at registration, and (c) they completed first-time readings of at least five distinct stories. Sessions will be excluded if any page's reading time falls below 2 seconds or exceeds 5 minutes, or if a page's implied rate exceeds 10 words per second, far above the roughly 80–170 words per minute typical of readers aged 7 and above [@hasbrouckOralReadingFluency2006]. These bounds indicate page-turning without reading or abandonment of the session. Sessions are further excluded if accounts are identified as duplicates via device fingerprinting, or if sessions were flagged by the app as incomplete (e.g., due to an app crash). Two properties of the registration data bound these criteria: the age band has no upper bound, and the logs identify accounts rather than readers, so sessions on a shared family device may include parental co-reading or use by siblings; both constraints are treated as measurement limitations and screened diagnostically (see Measures).

## Data Provenance and Researcher Access

Because the *Let's Story* logs were generated by ordinary app use before this Stage 1 submission, the study is a secondary analysis of pre-existing data, and we certify the research team's level of prior access in line with the Registered Reports requirements for existing data. At the time of submission the analytic dataset has not been extracted, and no member of the team has examined any relationship between the text-feature predictors and the reading-time outcome; the only previously known quantities are aggregate platform statistics already in the public domain, such as the total numbers of story sessions and generated stories [@ratnerDidRobotWrite2025]. For completeness, we disclose that a small-scale qualitative play-test of the *Let's Story* feature involving 22 parent–child pairs has been conducted within the same research partnership [@ratnerDidRobotWrite2025]; that study involved no access to the platform's behavioural logs, and no relationship between text properties and reading time was examined or observed [authors to confirm the final list of contributors with prior platform involvement]. Data extraction, computation of text features, and model fitting will be carried out only after in-principle acceptance. To protect the confirmatory tests from analytic flexibility, text-feature construction and the dimension-reduction step (see Analytic Plan) will be performed without reference to the outcome variable, so that decisions about which features to retain cannot be influenced by their association with reading time. Should any unanticipated prior access to the data come to light, it will be disclosed in the Stage 2 manuscript.

## Materials

The reading materials consist of short narrative passages (200–400 words each) drawn from the *Let's Story* library within the *Applaydu* app. These stories vary in vocabulary, syntax and coherence, providing natural variation in the text-level features of interest. Because the stories are generated by a large language model, their language is not a random sample of children's literature. In particular, LLM-generated children's stories tend to exceed age-appropriate vocabulary levels [@valentiniAutomaticGenerationSimplification2023]. The observed distribution of every text feature will therefore be reported, allowing readers to judge the difficulty range actually sampled.

For each story, we will compute computational text features organised into four theoretically motivated families and extracted with tools that operate across the languages represented in the corpus. Morphosyntactic annotation (tokenisation, lemmatisation, part-of-speech tagging, and dependency parsing) will be produced with Stanza, a neural pipeline providing Universal Dependencies models for more than 60 languages [@qiStanzaPythonNatural2020], so that the same operational definitions apply in every language. *Lexical-distributional features* include lexical diversity (the Measure of Textual Lexical Diversity [MTLD], type/token ratio, and lexical density) and log-transformed word frequency drawn from language-specific subtitle- and corpus-based norms (e.g., the SUBTLEX family and comparable resources) [@brysbaertMovingKuceraFrancis2009; @vanheuvenSubtlexUKNewImproved2014]; for languages with validated norms we additionally include age of acquisition and concreteness or imageability ratings. Within this lexical-distributional family we also include word-by-word surprisal, the negative log probability of each word given its preceding context, computed from an autoregressive language model (per-language Goldfish models; see @tbl-measures) and averaged across the story. This measure bridges the feature-based and neural approaches reviewed above. Surprisal is among the most reliable computational predictors of reading time, which increases linearly with surprisal (that is, logarithmically with word probability [@smithEffectWordPredictability2013; @shainLargescaleEvidenceLogarithmic2024]), as expected under expectation-based accounts of comprehension [@levyExpectationbasedSyntacticComprehension2008]. Language-model surprisal predicts reading times across typologically diverse languages [@devardaScalingCognitiveModelling2023], supporting its cross-linguistic use here, though direct evidence in child readers remains limited, a gap the present study helps to address. *Syntactic-structural features* include mean dependency distance, clause count per sentence, subordination ratio, mean sentence length, and noun-phrase complexity indices (determiners and dependents per NP), all computed from the Stanza dependency graphs. *Semantic and discourse-coherence features* are led by sentence-to-sentence cosine similarity computed from language-agnostic sentence embeddings (LaBSE), which represent sentences from 109 languages in a shared space [@fengLanguageagnosticBERTSentence2022], complemented where available by co-reference and argument overlap and by connective density (additive, causal, temporal, and adversative connectives) derived from language-specific connective inventories. *Affective and pragmatic features* include sentiment valence, emotional arousal, and cognitive or social word-category counts, computed from validated lexica in the languages for which they exist.

Two principles govern cross-linguistic feature construction. First, each feature is defined operationally on the output of a single multilingual pipeline — Stanza for morphosyntax and LaBSE for semantic similarity — so that, for instance, mean dependency distance denotes the same computation in Finnish as in Spanish. Second, lexicon-dependent features (frequency, age of acquisition, concreteness, sentiment) exist only for languages with validated norms; a feature that is unavailable or unreliable for a language is recorded as missing and removed during pre-screening if its missingness within that language exceeds the threshold set in the Analytic Plan. All features are standardised within language before dimension reduction, and a sensitivity analysis repeats the dimension reduction separately within each language (see Sensitivity Analyses) to confirm that the pooled cross-linguistic component structure adequately represents within-language variation. This feature set will be reduced to a smaller number of principal components prior to model fitting (see Analytic Plan).

### Operationalisation of Variables Across Languages

@tbl-measures specifies how each variable is operationalised and the source used to compute it. The guiding principle is to hold the measure constant across languages wherever a single validated cross-language tool or resource exists, and to fall back on language-specific validated norms (or to treat the feature as missing) only where it does not.

```{=latex}
\begingroup\setstretch{1.0}\small
```

| **Variable** | **Operationalisation** | **Source applied across languages** |
|---|---|---|
| Word frequency | Zipf-scaled log frequency, averaged over content words | `wordfreq` cross-language norms (subtitle-, Wikipedia- and web-based; 40+ languages) [@speerWordfreq2022], validated against native SUBTLEX databases for the most frequent languages [@brysbaertMovingKuceraFrancis2009; @vanheuvenSubtlexUKNewImproved2014] |
| Lexical diversity | MTLD (primary), type/token ratio, lexical density | MTLD index [@mccarthyMtldVocdHdd2010], computed on Stanza lemmas and part-of-speech tags |
| Age of acquisition | Mean rated AoA | Cross-language AoA ratings [@luniewskaRatingsAgeAcquisition2016] and native databases where available (e.g., English [@kupermanAgeofacquisitionRatings2012]); treated as missing where coverage is insufficient |
| Concreteness / imageability | Mean rated concreteness | Validated human norms where published (e.g., English [@brysbaertConcretenessRatings2014]); treated as missing otherwise |
| Word predictability | Mean per-word surprisal (negative log probability) | Comparably trained per-language autoregressive models covering 350 languages (Goldfish) [@changGoldfishMonolingual2024]; linear surprisal–reading-time relation, logarithmic in word probability [@smithEffectWordPredictability2013; @shainLargescaleEvidenceLogarithmic2024] |
| Dependency distance, clause count, subordination ratio, sentence length, NP complexity | Counts and ratios derived from dependency parses | Stanza neural pipeline [@qiStanzaPythonNatural2020] under the Universal Dependencies v2 scheme [@demarneffeUniversalDependencies2021] |
| Sentence-to-sentence coherence | Mean cosine similarity of adjacent sentences | Language-agnostic sentence embeddings (LaBSE) [@fengLanguageagnosticBERTSentence2022] |
| Co-reference and argument overlap | Lemma and named-entity overlap across sentences | Stanza lemmatisation and entity recognition [@qiStanzaPythonNatural2020] |
| Connective density | Counts of additive, causal, temporal and adversative connectives | Language-specific connective inventories (cf. [@crossleyToolAutomaticAnalysis2019]) |
| Valence and arousal | Mean rated valence and arousal | NRC VAD lexicon and its multilingual translations [@mohammadObtainingReliableHuman2018], validated against native affective norms where published (e.g., English [@warrinerNormsValenceArousal2013]) |
| Cognitive and social word categories | Proportional word-category counts | LIWC dictionaries — LIWC-22 for English [@boydDevelopmentPsychometricProperties2022], with published translations for other languages |

: Operationalisation of the Text-Feature Variables Across Languages {#tbl-measures tbl-colwidths="[22,30,48]"}

*Note.* Language-general measures — lexical diversity, all syntactic indices, coherence, and surprisal — are computed identically in every language by the same tools; lexicon-dependent measures use a cross-language resource as the primary source, supplemented by native validated norms where they exist. MTLD = Measure of Textual Lexical Diversity; AoA = age of acquisition; NP = noun phrase; LaBSE = language-agnostic BERT sentence embeddings; NRC VAD = NRC valence–arousal–dominance lexicon; LIWC = Linguistic Inquiry and Word Count.

```{=latex}
\endgroup
```

These resources cover the languages represented in the *Let's Story* corpus (see Participants). The language-general measures (lexical diversity, all syntactic indices, sentence-to-sentence coherence, and surprisal) are computed identically in every language by the same tools and therefore constitute a single measure applied across languages. Among the lexicon-dependent measures, word frequency (`wordfreq`) and valence and arousal (the NRC VAD lexicon and its translations) are likewise available from one cross-language resource for nearly every language in the sample (Thai lacks `wordfreq` coverage and its frequency is treated as missing; see the Appendix). Native validated norms (the SUBTLEX frequency databases and language-specific affective norms) are used for the five most frequent languages as a convergent-validity check. Age of acquisition, concreteness, and the LIWC word-category counts have validated coverage for only a subset of languages; where a norm is unavailable or unreliable for a language, the corresponding feature is recorded as missing and removed by the pre-screening rule (see Analytic Plan), and the per-language PCA sensitivity analysis confirms that such omissions do not distort the component structure. Where robust tokenisation or dependency parsing is unavailable for a language (for example, a language without reliable automatic word segmentation), the affected features are flagged and, if necessary, that language is excluded from the relevant analyses.

The *Let's Story* feature of Applaydu records several behavioural variables for each reading session: reading time (in seconds) per page and per story, interaction events (clicks, page flips, hint usage), and session metadata (language, an age flag, date/time). The platform does not record whether read-aloud narration was enabled, nor a comprehension measure that is comparable across the languages in the corpus, nor the auxiliary child- and family-level measures collected by other *Applaydu* activities (for example, a vocabulary score, a self-reported difficulty rating, a joint-engagement measure from shared play, or caregiver age). Child age is available only as a single "7 and older" band, with no finer resolution, so it serves solely as an inclusion criterion and cannot enter the models as a predictor or moderator. The analysis therefore rests on reading time and the session metadata, with reading time per word as the sole outcome.

## Procedure

Reading occurs under naturalistic, unconstrained conditions ("in the wild"). We will restrict the analysis to first-time readings of each story (excluding repeated readings) and require a minimum of 5 distinct stories per child. Data extraction will preserve the chronological order of sessions but exclude all personally identifying information. To reduce the influence of extreme outliers, reading time values will be winsorised at the 1st and 99th percentiles within each language group.

## Measures

The outcome is reading time per word, computed as total story reading time divided by word count. Because the platform records no comprehension measure that is comparable across the languages of the corpus, reading time per word is the sole outcome and no secondary comprehension outcome is analysed.

The predictor variables fall into two categories. Text features comprise the four families of computational indices described in Materials; these will be reduced to principal component scores before entering any model (see Dimension Reduction via Principal Component Analysis). The reader feature is within-child reading experience, operationalised primarily as the lagged, log-transformed cumulative number of words the child has read in prior sessions (with session order, the sequence position, retained as a robustness coding).

The resulting dataset has a multilevel structure. At Level 1, observations correspond to individual story-reading instances (one child reading one story). At Level 2, observations are nested within children. At Level 3, children are nested within languages. Because the text features are standardised within language before dimension reduction, the resulting principal component (PC) scores are centred within language by construction: text-feature effects are estimated from within-language variation between stories, and between-language differences in average text difficulty are absorbed by the Language random effects. Reading experience (cumulative words read) will be centred within child (person-mean centred) and divided by its pooled within-child standard deviation, so that its effect is estimated purely within child, is separated from between-child differences in total app usage, and is expressed on the same per-standard-deviation scale as the component scores. The region of practical equivalence is defined on this scale (see Analytic Plan).

Four properties of the data condition the inferences these measures can support. First, reading time per word indexes reading effort only to the extent that the child sets the pace and engages with the text; we therefore verify that recorded reading time covaries with text length as a precondition for analysis (see Outcome-Neutral Quality Checks and Positive Controls), since without that relationship recorded times could not be read as an index of engagement with the text. Relatedly, reading time per word indexes processing effort and developing fluency rather than comprehension itself: children's reading rate relates reliably to overall reading competence [@fuchsOralReadingFluency2001; @sortkaerBothSidesStory2021], but reading speed is not a consistent within-reader predictor of comprehension [@wallotRoleReadingTime2014], so the confirmatory inferences concern reading behaviour, and comprehension-level claims lie outside their scope. Second, because the data are observational, neither the child's language nor the amount the child has read is experimentally assigned, and accumulated reading experience is further confounded with selective retention, in that children who remain in the app read more. We therefore frame the within-child experience effect as associational rather than causal, and we report the selection limitation explicitly [@kraemerHowCanWe2000]. The effect is estimated within child and net of the text-feature components, and is accompanied by a completer-only sensitivity analysis (see Sensitivity Analyses). Third, stories can be read aloud by the app's AI narrator, and the logs do not record whether narration was enabled, so for an unknown fraction of sessions per-word times may reflect the narrator's pacing rather than the child's. Because text-to-speech pacing is far less sensitive to a text's processing difficulty than child-paced reading, undetected narration would attenuate the text-feature effects towards zero, rendering the H1 tests conservative rather than anticonservative, while also compressing the within-child variation on which H2 relies. A pre-specified diagnostic therefore screens for narration-paced sessions (see Outcome-Neutral Quality Checks), and a sensitivity analysis excludes them. Fourth, the logs identify accounts rather than readers: the age band is parent-reported at registration with no upper bound, and sessions on a shared family device may include parental co-reading or use by siblings. We therefore interpret effects at the level of account-holders registered in the 7-and-older band, treat reader identity as a source of outcome noise that the very large sample attenuates but does not remove, and screen the per-language distribution of per-word reading rates against published child reading-rate ranges [@hasbrouckOralReadingFluency2006], reporting the proportion of sessions faster than plausible child-paced reading. These constraints define the causal scope of the confirmatory tests and are revisited when each hypothesis is interpreted.

@fig-mindmap sets out the theoretical scope of the study: the state-of-the-art findings the theory builds on, its constructs and labelled propositions, and the pre-registered predictions that the confirmatory analyses test, together with the alternative accounts that the same tests are designed to discriminate against.

::: {#fig-mindmap}

::: {.content-visible when-format="pdf"}
![](../mindmap/mindmap.pdf){width=100%}
:::

::: {.content-visible unless-format="pdf"}
{{< include ../mindmap/mindmap.svg >}}
:::

The Theoretical Scope of the InTRACt Study
:::

```{=latex}
\begingroup\setstretch{1.1}\small\vspace{-14pt}
```

::: {style="line-height: 1.45; margin-top: -0.4em; font-size: 0.95em;"}
*Note.* Generated with theoryforge from the study's machine-readable theory specification. The top band lists the findings the theory builds on; the middle band states the constructs and the labelled propositions P1 to P6, with the TEXT, READER and CONTEXT tags marking the study's three dimensions; the bottom band states the pre-registered predictions (Hypotheses 1a, 1b, 1c and 2), each able to fail and each pointed diagnostically at an alternative account. The alternative accounts run through the same pre-registered tests (the narration-pacing screen, the engagement check, and the completer-only analysis), so the evidence decides among explanations rather than confirming one. LexDistPC = lexical-distributional component; SyntaxPC = syntactic-structural component; CoherencePC = discourse-coherence component; HDI = highest-density interval; ROPE = region of practical equivalence.
:::

```{=latex}
\endgroup
```

## Analytic Plan

All confirmatory analyses will use Bayesian hierarchical models fitted with the `brms` package [@burknerBrmsPackageBayesian2017], which implements full Bayesian inference via Hamiltonian Monte Carlo (No-U-Turn Sampler). We adopt this framework in preference to null-hypothesis significance testing on three grounds. Our scientific questions concern the *magnitude* and *direction* of effects rather than their mere existence. The expected sample size gives effectively unlimited frequentist power, which makes *p*-value thresholds uninformative. A single unified framework also avoids the family-wise error inflation that arises from conditional multi-stage testing. Prior distributions are specified to be genuinely informative; their motivation and the prior placed on each parameter class are set out under Prior Specification below.

For each pre-registered hypothesis we will report, unconditionally and regardless of outcome, the posterior mean and 95% highest-density interval (HDI) for the relevant parameter; the posterior probability that the parameter falls in the predicted direction, P($\beta$ < 0) or P($\beta$ > 0) as specified per hypothesis; and the position of the 95% HDI relative to a region of practical equivalence (ROPE) [@kruschkeRejectingAcceptingParameter2018] defined as $|\beta| < 0.05$ on the standardised scale, the threshold below which we consider an effect too small to be educationally meaningful. Each directional hypothesis is evaluated with the HDI+ROPE decision rule: a hypothesis is supported if the 95% HDI lies entirely outside the ROPE in the predicted direction; disconfirmed if the HDI lies entirely outside the ROPE in the opposite direction; the effect is declared practically equivalent to zero if the HDI lies entirely within the ROPE; and the result is otherwise inconclusive. Both confirmatory hypotheses are evaluated under this framework simultaneously and unconditionally, with no first-stage filter; the three text-feature sub-hypotheses (H1a–H1c; see Hypotheses) are estimated jointly in a single model and evaluated independently, with results reported per sub-hypothesis and no omnibus claim about H1 as a whole. Four chains of 4,000 iterations each (2,000 warm-up) will be run, with $\hat{R}$ < 1.01 and effective sample size > 1,000 per parameter as convergence diagnostics. Models will be fitted with the `cmdstanr` backend using within-chain parallelisation (`reduce_sum`), with the number of threads per chain fixed in the registered code; if a confirmatory model has not completed sampling within 14 days on the available high-performance-computing allocation, it will instead be fitted to a random subsample of children (100,000 children, drawn proportionally to language, with the sampling seed fixed in the registered code). This contingency is purely computational, is specified blind to any outcome, and leaves the inference criteria unchanged.

### Prior Specification

We specify genuinely informative, regularising priors. This departs from the diffuse or default priors common in applied work; the default `brms` prior on population-level coefficients, for instance, is an improper flat prior that imposes no regularisation [@burknerBrmsPackageBayesian2017]. This choice is motivated on two grounds. First, priors that concentrate mass on plausible parameter values regularise the posterior geometry and thereby improve Hamiltonian Monte Carlo sampling, reducing divergent transitions, raising effective sample sizes, and shortening warm-up and total sampling time. This illustrates the principle that computational difficulties usually signal an under-constrained model rather than a purely numerical obstacle [@gelmanBayesianWorkflow2020; @betancourtConceptualIntroductionHamiltonian2017]. For a maximal random-effects structure over many-levelled and partly sparse Child and Language groupings, this regularisation is also what makes a complex model estimable within a feasible time frame, and more informative priors are known to speed convergence in exactly such hierarchical, reading-time models [@schadPrincipledBayesianWorkflow2021; @mcelreathStatisticalRethinking2020]. Second, although the very large sample means the likelihood will dominate the posterior for the main effects [@lemoineMovingBeyondNoninformative2019], the priors still do real work in the sparsely populated parts of the design (small language strata and the by-child random slopes), where they guard against implausibly large estimates.

Because the predictors are standardised and reading time is log-transformed, the fixed-effect coefficients are interpretable on a per-standard-deviation scale, on which reading-research effects are modest: word frequency and predictability enter reading time approximately logarithmically and with small per-unit slopes [@shainLargescaleEvidenceLogarithmic2024], and app-based reading-speed effects are moderate (*r* ≈ .39–.48) [@sortkaerBothSidesStory2021]. We therefore place Normal(0, 0.2) priors on the population-level coefficients. Normal rather than Student's *t* priors are used for these coefficients because their log-concave tails additionally aid sampling [@gelmanBayesianWorkflow2020]. Because both hypotheses are evaluated confirmatorily and unconditionally, every prior is centred at zero and symmetric: it regularises through its scale alone and does not encode the predicted direction, so the data remain free to disconfirm each hypothesis [@schadPrincipledBayesianWorkflow2021]. Group-level standard deviations (for Child, Language, and the random slopes) receive half-Normal(0, 1) priors, as does the residual standard deviation; the random-effect correlation matrices receive LKJ($\eta$ = 2) priors, which place the mode at the identity matrix and gently discourage the near-perfect random-slope correlations that drive divergences in maximal models [@lewandowskiGeneratingRandomCorrelation2009; @burknerBrmsPackageBayesian2017].

All priors will be examined with prior predictive simulations before any model is fitted to the data, and their influence will be quantified in a pre-registered prior-sensitivity analysis that compares them against the weakly informative alternatives (see Sensitivity Analyses), in line with recommended Bayesian-workflow practice [@vandeschootBayesianStatisticsModelling2021; @schadPrincipledBayesianWorkflow2021].

### Sampling Plan and Inferential Sensitivity

Because the study analyses pre-existing observational data, the sampling plan is defined by the inclusion and exclusion criteria (see Participants) rather than by recruitment, and the analytic sample (first-time story readings nested within children, who are nested within languages) is expected to be very large: in its first six months the platform recorded over 2.4 million story sessions and more than 40,000 generated English-language stories [@ratnerDidRobotWrite2025]. The exact analysable *N* is fixed by the pre-registered filters and is unknown until extraction. At this scale the binding constraint on inference is the precision of estimation and the *practical* magnitude of effects, rather than statistical power: even negligible effects attain conventional significance when *N* is in the millions [@gelmanBeyondPowerCalculations2014]. We therefore replace a conventional power analysis with a precision-based Bayesian design analysis organised around the region of practical equivalence (ROPE) and the highest-density-interval decision rule [@kruschkeRejectingAcceptingParameter2018].

Prior to fitting any model to the real data, we will conduct a simulation-based design analysis, fully specified here so that its adequacy can be assessed before any outcome is known. We will generate 500 synthetic datasets per cell of a grid crossing analysable *N* ∈ {50,000; 200,000; 1,000,000} observations with standardised effect sizes $\beta$ ∈ {0, 0.03, 0.05, 0.10, 0.20}, spanning the null, the ROPE bound, and the range of plausible effects anchored by the closest empirical estimates: the reading-speed–ability association (*r* ≈ .39–.48) reported by @sortkaerBothSidesStory2021 and typical syntactic-complexity effects on children's reading (*d* ≈ 0.2–0.5). Observations are allocated across languages in proportion to the platform's published language distribution, and per-child session counts are drawn from a truncated negative binomial calibrated to the published aggregate of 2.4 million sessions. The generative model is structurally identical to the confirmatory model, including its random-effects structure, story-level variability, and priors, with random-effect standard deviations set from the same empirical anchors. The design-analysis code and seeds will be deposited with this protocol, and the resulting table of expected 95% HDI widths and probabilities of a conclusive ROPE decision per grid cell will accompany the Stage 1 submission [design-analysis table to be inserted after simulation]. The same simulation identifies the minimum analysable *N* at which a conclusive decision is reached with at least 90% probability for each parameter; if extraction yields fewer eligible observations for a given hypothesis (for example, within smaller language groups), that hypothesis will be flagged as insufficiently sensitive and interpreted descriptively rather than confirmatorily.

### Outcome-Neutral Quality Checks and Positive Controls

To confirm that the data and the analysis pipeline can detect known effects before the focal hypotheses are evaluated, we pre-specify the following outcome-neutral checks. First, as a validity check on the timing measure, total story reading time must increase with story word count; the check passes if, within each of the five largest languages, the posterior slope of log total reading time on log word count is positive with posterior probability of at least .95. Its absence would indicate that recorded times do not reflect engagement with the text. Second, as a positive control on the feature pipeline, we will fit a minimal ancillary model predicting log reading time per word from story-mean Zipf word frequency alone (with random intercepts for Child and Language), separate from the PCA-based confirmatory models; word frequency is the most robust and cross-linguistically replicable lexical predictor of reading time [@kupermanWordLength2024; @vanheuvenSubtlexUKNewImproved2014], and the control passes if the posterior probability of a negative frequency slope exceeds .95. Because word frequency also loads on the lexical-distributional component, this control is not independent of H1's lexical sub-hypothesis (H1b): it is therefore treated as a pipeline check on which the syntactic (H1a) and coherence (H1c) tests are conditioned, while the lexical test (H1b) is interpreted jointly with it. Third, the per-language distribution of reading time per word will be screened for floor and ceiling effects and for adequate variance; an outcome lying at floor or ceiling for more than 90% of observations within a language will not be modelled in that language. Fourth, because narration status is unrecorded (see Measures), we will screen for narration-paced sessions: text-to-speech pacing is nearly constant per word, so for each language we will inspect the distribution of per-word times for a low-variance mode at a language-consistent rate; sessions falling within such a mode will be flagged, the flagged proportion reported, and the confirmatory models re-fitted without them as a pre-registered sensitivity analysis (see Sensitivity Analyses). These checks are evaluated before the confirmatory models; where a check fails, the dependent confirmatory test is reported as uninterpretable rather than as evidence for or against the corresponding hypothesis.

### Dimension Reduction via Principal Component Analysis

Before fitting any mixed-effects model, we will reduce the full set of text-level features to a smaller number of stable, interpretable components using PCA, following the approach of @crossleyDevelopingLinguisticConstructs2025. PCA is chosen over exploratory factor analysis because the goal is dimension reduction of observed stimulus properties rather than estimation of unobserved latent causes: there is no theoretical basis for claiming that a latent factor *causes* MTLD, dependency distance and connective density to co-vary, and treating them as reflective indicators of a latent construct would misrepresent the nature of computational linguistic indices [@crossleyDevelopingLinguisticConstructs2025].

Prior to PCA, variables will pass through several pre-screening stages. The pooled confirmatory PCA is restricted to the features available in every analysed language: word frequency, surprisal, the lexical-diversity indices, the syntactic indices, sentence-to-sentence coherence, connective density, and valence and arousal. Lexicon-dependent features without full cross-language coverage (age of acquisition, concreteness/imageability, and the LIWC categories) enter only the per-language sensitivity PCAs for the languages in which validated norms exist (see the Appendix), so that the appendix norms serve the sensitivity and convergent-validity analyses rather than the pooled solution. Within that set, variables with more than 20% missing values in any language, near-zero variance (SD < 0.01 after within-language standardisation), or excessive zero inflation (more than 20% zero counts) will be excluded. Variables showing absolute skewness or excess kurtosis greater than 3 [@crossleyDevelopingLinguisticConstructs2025] will first be transformed (log(x + c) or Yeo–Johnson) and excluded only if the transformed variable still breaches these bounds, so that naturally skewed but theoretically central features (such as connective density in short stories) are not discarded where a transformation suffices. Where two variables correlate at $r$ > .90, we will retain the variable with stronger theoretical motivation, better cross-linguistic availability, lower missingness, or clearer interpretability. This selection will not consult the variable's correlation with the outcome, preventing outcome information from leaking into feature construction.

All retained variables will be $z$-scored within language before PCA, removing between-language scale differences while preserving within-language variation. The primary analysis pools all language-standardised variables into a single PCA conducted in R using the `psych` package [@revelleRPackagePsych2023], after verifying the Kaiser–Meyer–Olkin (KMO) measure of sampling adequacy (individual KMO > .50; overall KMO > .60); variables with individual KMO below .50 are removed iteratively, lowest first, with the KMO recomputed at each step. Bartlett's test of sphericity is reported for completeness only, since at this sample size it rejects trivially and carries no decision weight. We use oblique rotation (promax, $\kappa$ = 4) rather than varimax, because linguistic features are naturally correlated and an orthogonality assumption is theoretically unjustified [@biberVariationSpeechWriting1988; @crossleyDevelopingLinguisticConstructs2025]. Components will be retained based on three jointly applied criteria: parallel analysis [@hornRationaleTestNumber1965], which retains components whose eigenvalues exceed the 95th percentile from 10,000 matched random data structures; scree-plot inspection; and interpretability, with a component accepted only if the pattern matrix admits a coherent reading-theoretic interpretation. Variables with absolute pattern loadings below .30 will not be attributed to any component. Component scores for each retained component will then be computed per story by the regression method (`psych::principal`) and will serve as text-level predictors in the confirmatory models below. The full pattern matrix (all loadings ≥ .15), component-score intercorrelations, proportion of variance per component, and per-variable KMO values will be reported in a supplementary table, with each component named based on its highest-loading variables and theoretical interpretation.

### Confirmatory Models

The confirmatory model predicts log-transformed reading time per word from the retained text-feature PC scores and within-child reading experience (lagged log cumulative words read, person-mean-centred and scaled; see Measures). The log transformation normalises the positively skewed distribution, and posterior predictive fit is verified via `pp_check` plots. The random-effects structure follows a maximal justified specification [@barrRandomEffectsStructure2013], with random intercepts for Child and Language, random slopes for the hypothesis-bearing PC components and for reading experience within Child, and random slopes for the hypothesis-bearing components within Language (e.g., `(1 + LexDistPC + SyntaxPC + CoherencePC + Experience | Child) + (1 + LexDistPC + SyntaxPC + CoherencePC | Language)`), so that the fixed text-feature effects are estimated as averages over languages rather than dominated by the largest language groups; the posterior standard deviations of the by-language slopes are reported as the pre-registered index of cross-linguistic consistency. Because stories are generated uniquely for each family, most stories are expected to be read by a single child, and text-feature effects are identified predominantly from variation across each child's own stories; at extraction we will report the proportion of observations contributed by stories read by more than one child, and if it exceeds 5% a crossed random intercept for Story will be added to account for shared story-level variability [@juddTreatingStimuliRandom2012]. The confirmatory ROPE/HDI decisions are taken from the winsorised, untrimmed model; as a sensitivity analysis, observations with standardised residuals exceeding ±3 *SD* based on posterior predictive draws will be removed once, without iteration, and the model re-fitted (see Sensitivity Analyses).

### Convergence and Multicollinearity

A random intercept for *Language* will be included in all models to account for between-language variability and enable partial pooling. Non-convergence is defined as any $\hat{R}$ ≥ 1.01, bulk or tail effective sample size below 1,000 on a reported parameter, or more than 0.1% divergent transitions. We will first increase `adapt_delta` to 0.95 and then 0.99 (with `max_treedepth` = 12); only if diagnostics still fail will we simplify the structure, removing the random slope with the smallest posterior standard deviation (by-child slopes before by-language slopes, and never a term required to identify a hypothesis), iterating until convergence and reporting every step. Residual multicollinearity among the retained PC scores will be assessed using variance inflation factors (VIF) on the fixed-effects design matrix after the dimension-reduction step. No hypothesis-bearing component will be removed on collinearity grounds: if any VIF exceeds 5, any retained component not named in a hypothesis is removed first, and if collinearity among SyntaxPC, LexDistPC and CoherencePC alone drives a VIF above 10, the affected sub-hypothesis is declared untestable on collinearity grounds and the joint posterior of the correlated coefficients is reported instead.

### Model Comparison

We will compare the full model against reduced models that drop the text-feature components and the reading-experience term in turn, using K-fold cross-validation grouped by child (K = 10; `brms::kfold`), which targets the relevant generalisation — prediction for new children — and avoids storing the pointwise log-likelihood matrix that exact leave-one-out cross-validation would require at this scale [@vehtariPracticalBayesianModel2017]. We report the expected log predictive density (ELPD) difference and its standard error; a model is preferred only if the ELPD difference exceeds four and is at least twice its standard error, with smaller differences reported as predictively indistinguishable. Cross-validation is preferred over likelihood-ratio tests because it requires no asymptotic approximations and is well-calibrated within the Bayesian framework adopted here.

### Minimum Effect Size and ROPE Calibration

ROPE bounds are informed by standardised effect sizes from the closest available prior studies: @sortkaerBothSidesStory2021 reported $r$ ≈ .39–.48 for reading-speed effects, and syntactic complexity effects on children's reading times in eye-tracking studies are typically in the range $d$ = 0.2–0.5. A standardised $\beta$ of 0.05 is therefore conservatively below the range of effects the existing literature considers educationally meaningful. The same ±0.05 bound applies to the H2 experience slope, which is scaled to the per-standard-deviation metric before model entry (see Measures). Sensitivity analyses varying the ROPE bound to ±0.03 and ±0.10 will assess the robustness of practical-equivalence conclusions.

### Sensitivity Analyses

Ten pre-registered sensitivity analyses will assess the robustness of the main findings. First, the primary models will be re-fitted without any outlier exclusion criteria, to establish whether winsorisation drives any substantive conclusion. Second, complementing this from the other side, observations with posterior-predictive standardised residuals beyond ±3 *SD* will be removed in a single pass and the models re-fitted (see Confirmatory Models). Third, the models will be restricted to the five largest language groups, to evaluate cross-linguistic generalisability independently of less-represented languages. Fourth, the ROPE bounds will be varied to ±0.03 and ±0.10 as described above. Fifth, the confirmatory models will be re-estimated using sparse PCA scores (obtained via the R package `sparsepca`, with sparsity regularisation selected by cross-validation) to assess whether structurally simpler components with fewer non-zero loadings alter the substantive pattern of results. Sixth, language-specific PCAs will replace the pooled solution, with one PCA fitted per language (attempted only for languages with at least 300 stories), to test whether the cross-lingual feature space adequately represents within-language variation. Congruence between each per-language solution and the pooled solution will be quantified with Tucker's congruence coefficient, taking $\varphi$ ≥ .85 as fair similarity and $\varphi$ ≥ .95 as equivalence [@lorenzosevaTuckersCongruenceCoefficient2006], and the confirmatory models will be re-fitted excluding any language whose hypothesis-bearing components fall below .85. Seventh, the sample will be restricted to children who completed a higher minimum number of sessions (a completer-only subsample), to assess whether selective retention biases the within-child reading-experience effect (H2). Eighth, a prior-sensitivity analysis will re-fit the confirmatory models under the previously specified weakly informative priors (Student's *t*(3, 0, 2.5) on the fixed effects). Because at this sample size the group-level priors do the real regularising work, the same analysis will also apply half-Normal(0, 0.5) and Exponential(1) alternatives to all group-level and residual standard deviations, and LKJ($\eta$ = 1) and LKJ($\eta$ = 4) alternatives to the correlation matrices, to confirm that the substantive conclusions are driven by the data rather than by the priors [@vandeschootBayesianStatisticsModelling2021]. Ninth, to separate within-child experience from platform-level drift (app updates, changes to the story generator) and from seasonal variation, the H2 model will be re-fitted with a spline of calendar week since feature launch and with days-since-first-session (person-mean-centred) as an additional within-child covariate; H2 is interpreted as robust only if the experience effect persists. Tenth, the confirmatory models will be re-fitted excluding the sessions flagged by the narration-pacing screen (see Outcome-Neutral Quality Checks and Positive Controls).

### Supplementary Frequentist Models

To facilitate comparison with prior literature and to assist readers unfamiliar with Bayesian inference, fixed-effect point estimates, standard errors, and confidence intervals from a maximum-likelihood linear mixed model (LMM; fitted with `lme4`/`lmerTest`, identical random-effects structure) will be reported in a supplementary table. These results are descriptive only and carry no confirmatory weight; all hypothesis evaluation is conducted solely via the Bayesian framework described above.

# Hypotheses

Our hypotheses are directional and each maps onto a specific fixed-effect term in the confirmatory model described above. Following the PCA procedure described in the Analytic Plan (see Dimension Reduction via Principal Component Analysis), hypotheses about text-level features are stated in terms of the PC component families expected to carry the relevant variance. The syntactic-structural component is referred to as SyntaxPC; the semantic/coherence component as CoherencePC; and the lexical-distributional component as LexDistPC. Because exact component names will be assigned post-hoc from the pattern matrix, each directional prediction is conditional on a component with the expected feature profile emerging from the PCA. To remove analyst discretion from that step, component matching is algorithmic, with marker variables fixed in advance: for SyntaxPC, mean dependency distance, subordination ratio, and clause count per sentence; for CoherencePC, sentence-to-sentence embedding similarity, argument overlap, and connective density; and for LexDistPC, Zipf word frequency (reverse-scored), surprisal, and MTLD. A retained component is designated as a hypothesised component if and only if it is the retained component with the highest mean absolute pattern loading on that family's markers and at least two of the three markers load on it at ≥ .30 with a consistent sign. If no retained component satisfies this rule for a family, the corresponding sub-hypothesis will be reported as untestable.

First, we predict that text-level features predict reading time: stories scoring higher on syntactic and lexical difficulty (longer dependency distances, less frequent words, higher lexical diversity and lower coherence) should be associated with longer reading times per word. We expect (H1a) a main effect of SyntaxPC in the direction of longer reading times; (H1b) a main effect of LexDistPC in the direction of longer reading times; and (H1c) a main effect of CoherencePC in the direction of shorter reading times. The three sub-hypotheses are estimated jointly in a single model and evaluated independently: support for any subset is reported as such, and no omnibus claim is made about H1 as a whole.

Second, we predict that children read progressively faster as they accumulate reading experience within the app. Reading practice builds fluency and lowers reading time over time, as indicated by app-based reading logs and longitudinal studies of reading experience [@sortkaerBothSidesStory2021; @schmidtkeReadingExperience2024], so reading time per word should decrease as a child reads more. We operationalise within-child experience primarily as the cumulative number of words the child has read in prior sessions, lagged so the current story is excluded and log-transformed to capture the diminishing-returns shape characteristic of practice curves [@loganInstanceTheoryAutomatization1988]. The measure is centred within child (person-mean), so that the effect is estimated purely within child and is not confounded with between-child differences in total app usage [@kraemerHowCanWe2000]. The simpler session-order count (sequence position) is retained as a robustness coding. We use cumulative words rather than cumulative reading time, because the latter is constructed from the dependent variable. We expect a negative main effect of accumulated reading on reading time, with a by-child random slope capturing individual differences in practice rate. Because retention in the app is self-selected, this effect is associational (estimated within the children who remain and net of the text-feature components already in the model) and is accompanied by a completer-only sensitivity analysis (see Sensitivity Analyses).

Each hypothesis will be evaluated through the corresponding mixed-model term using the Bayesian inference criteria described in the Analytic Plan (posterior mean, 95% HDI, directional posterior probability, and ROPE analysis). Both hypotheses are evaluated under the same inferential framework, applied unconditionally and simultaneously.

{{< pagebreak >}}

## Study Design Table

```{=latex}
\begingroup\setstretch{1.0}\small
```

| **Research question** | **Hypothesis (predicted effect)** | **Test (model term and inference)** | **Interpretation of possible results** |
|---|---|---|---|
| RQ1 — text properties predict reading time per word | **H1 (three sub-hypotheses, evaluated independently).** H1a: $\beta$(SyntaxPC) > 0; H1b: $\beta$(LexDistPC) > 0; H1c: $\beta$(CoherencePC) < 0. | Main effects in the primary reading-time model (log reading time per word); posterior mean, 95% HDI, directional probability, and HDI+ROPE rule. All conditional on the word-count validity check; H1a and H1c additionally conditional on the ancillary frequency control, with which H1b is interpreted jointly. | For each sub-hypothesis: a 95% HDI outside the ROPE in the predicted direction supports it; an HDI outside the ROPE in the opposite direction disconfirms it; an HDI inside the ROPE indicates no practically meaningful effect; a straddling HDI is inconclusive. No omnibus H1 claim is made. |
| RQ2 — reading changes with accumulated experience | **H2.** Reading speeds up as within-child experience accumulates: $\beta$(Experience) < 0 on reading time. | Main effect of within-child reading experience (lagged log cumulative words read, person-mean-centred and scaled) in the reading-time model, with a by-child random slope; posterior mean, 95% HDI, directional probability, and HDI+ROPE rule. Associational, with completer-only, calendar-time, and narration sensitivity analyses. | An HDI below the ROPE in the negative direction supports the associational claim of H2 — faster reading with accumulated exposure — consistent with growing fluency; because increased skimming would produce the same pattern, this reading is corroborated or qualified by the engagement check (see Exploratory Analyses). An HDI inside the ROPE indicates no practically meaningful experience effect; a straddling HDI is inconclusive. |

: Design Table Linking Research Questions, Hypotheses, Tests, and Interpretation of Outcomes {#tbl-design tbl-colwidths="[18,32,28,22]"}

*Note.* All confirmatory tests use the Bayesian multilevel models and the HDI+ROPE decision rule set out in the Analytic Plan. Sampling adequacy for each test is established by the design analysis (see Sampling Plan and Inferential Sensitivity), and every test is conditional on the outcome-neutral quality checks. HDI = highest-density interval; ROPE = region of practical equivalence; PC = principal component.

```{=latex}
\endgroup
```

## Exploratory Analyses

Beyond the confirmatory tests above, we plan several analyses that are explicitly designated as exploratory and will be reported separately.

We will examine whether the magnitude of text-feature effects on children's reading varies systematically across the 19 languages represented in the *Let's Story* dataset. This could be addressed through language-stratified models or through meta-regression with typological features (e.g., orthographic depth) as moderators. Such analyses may reveal whether the relationships between, say, syntactic complexity and reading time are consistent across typologically diverse languages or are modulated by language-specific properties.

We will also explore whether text complexity features show non-linear (quadratic) relationships with reading time, which would suggest diminishing or accelerating effects at extreme levels of complexity.

Two further exploratory analyses build on the confirmatory within-child experience effect (H2). First, although the reviewed evidence that reading experience attenuates sensitivity to text difficulty concerns between-child skill differences rather than within-child change over a short window, we will explore whether accumulated experience moderates the text-feature effects, most plausibly attenuating the lexical-distributional cost, in line with the smaller lexical and word-length effects shown by more fluent and experienced readers [@lubineauExaminingImpactReading2024]. We remain agnostic about the syntactic and coherence components, for which the reviewed corpus is silent. Second, because practice curves are typically non-linear, we will compare the primary log-cumulative-words parameterisation against linear and by-child smooth (generalised additive) alternatives, and against the simpler session-order coding [@baayenNoteModelingEffects2021]. Throughout, accumulated reading is treated cautiously: it is confounded with selective retention and with drift in the difficulty of the stories served, so we verify that the confirmatory text-feature effects are unchanged when the experience term and its by-child slope are included, alongside the completer-only check reported under Sensitivity Analyses.

Two further exploratory checks probe the measurement basis of the confirmatory analyses. As an engagement check on H2, we will test whether per-session interaction density (page flips, clicks, and hint usage per page) declines with accumulated experience in parallel with reading time; a decline in reading time without a parallel decline in engagement is consistent with growing fluency, whereas parallel declines would qualify that interpretation towards disengagement or skimming. As a robustness check on the outcome's level of aggregation, the primary model will also be re-fitted to page-level per-word reading times, which preserve within-story variation that the story-level aggregate removes.

All exploratory results will be clearly labelled as such in the manuscript, and we will not draw confirmatory conclusions from these analyses.

# Declarations {.unnumbered}

## Ethics {.unnumbered}

This study is a secondary analysis of anonymised behavioural data generated during ordinary use of a commercial application; parental or guardian consent for data collection was registered in the app at account creation, under the platform's terms of use and privacy policy. No data are collected from children specifically for this study beyond those produced by normal app use, and the research team accesses no personally identifying information. Ethical review for the secondary analysis [has been / will be] obtained from the Departmental Research Ethics Committee of the University of Oxford's Department of Education, operating under the Central University Research Ethics Committee (CUREC) framework [approval reference to be inserted]. Processing of these data, which concern minors, complies with the applicable data-protection law (including the UK General Data Protection Regulation); the data controller for the source data is [Ferrero International and/or Gameloft — to be confirmed].

## Data and Code Availability {.unnumbered}

In keeping with the Registered Reports format, this Stage 1 protocol will be deposited in a public repository and made available either immediately or upon Stage 2 acceptance. All analysis code, the story-level computational text-feature matrices, and the aggregated data required to reproduce the reported results will be made openly available (for example, on the Open Science Framework) at Stage 2. The raw, individual-level reading logs are the property of the data controller ([Ferrero International and/or Gameloft]) and contain data from minors; their release is therefore governed by the data-sharing agreement and by data-protection law, and they will be made available from the data controller on reasonable request to the extent those constraints permit [author to confirm what may be shared]. This statement complements the outcome-blind data-access certification given under Data Provenance and Researcher Access.

## Funding {.unnumbered}

This work is supported by Kinder and the John Fell Fund (University of Oxford) [grant numbers to be inserted]. The behavioural data analysed here were generated by the *Applaydu* platform in the course of its normal operation, independently of this study and prior to its conception. Beyond providing access to these pre-existing, anonymised data, the funders and the data provider had no role in the design of the study, in the analysis or interpretation of the data, or in the decision to submit the work for publication.

## Competing Interests {.unnumbered}

The behavioural data analysed here are generated by *Applaydu*, an application developed by Gameloft and Ferrero International, and the research is funded in part by Kinder, a brand owned by Ferrero. This dual relationship, in which the funder and the provider of the data are the same commercial group whose product is under study, constitutes a potential competing interest, which the authors declare. Any further interests (for example, employment, consultancy, or other financial relationships with Ferrero or Gameloft) must also be declared; if none exist beyond the funding and data-provision relationship described here, this should be stated explicitly [author to confirm].

# Appendix: Per-Language Norm Sources {.unnumbered}

For the lexicon-dependent measures (frequency, age of acquisition, concreteness/imageability, valence/arousal, and the LIWC word-category counts), the validated native norms listed in @tbl-norms will be used wherever they exist. Every other language–measure combination defaults to the cross-language resource specified in @tbl-measures: `wordfreq` for frequency [@speerWordfreq2022], the NRC VAD lexicon for valence and arousal [@mohammadObtainingReliableHuman2018], and the 25-language set of @luniewskaRatingsAgeAcquisition2016 for age of acquisition. Where no validated norm is available, the combination is instead recorded as missing and removed by the pre-screening rule (see Analytic Plan). Native frequency norms are additionally used to validate the `wordfreq` estimates. The language-general measures (lexical diversity, all syntactic indices, coherence, and surprisal) are computed identically across languages and are not repeated here.

```{=latex}
\begingroup\setstretch{1.0}\small
```

| **Language** | **Validated native norms (cross-language defaults used otherwise)** |
|---|---|
| English | Frequency, SUBTLEX [@vanheuvenSubtlexUKNewImproved2014]; age of acquisition [@kupermanAgeofacquisitionRatings2012]; concreteness [@brysbaertConcretenessRatings2014]; valence and arousal [@warrinerNormsValenceArousal2013]; LIWC-22 dictionary [@boydDevelopmentPsychometricProperties2022]. |
| Spanish | Frequency, EsPal [@duchonEspalOnestopShopping2013]; age of acquisition [@alonsoSubjectiveAgeofacquisition2015]; valence and arousal [@stadthagengonzalezNormsValenceArousal2017]; LIWC Spanish dictionary. |
| German | Frequency, SUBTLEX-DE [@brysbaertWordFrequencyEffect2011]; valence, arousal and imageability from BAWL-R [@voBerlinAffectiveWordList2009] and the automatically generated norm set [@koperAutomaticallyGeneratedAffective2016]; LIWC German dictionary. |
| Dutch | Frequency, SUBTLEX-NL [@keuleersSubtlexnlNewMeasure2010]; age of acquisition and concreteness [@brysbaertNormsAgeAcquisitionConcreteness2014]; valence and arousal [@moorsNormsValenceArousal2013]; LIWC Dutch dictionary. |
| French | Frequency, Lexique [@newLexiqueBaseDonnees2001]; age of acquisition [@ferrandAgeofacquisitionSubjectiveFrequency2008]; imageability [@desrochersSubjectiveFrequencyImageability2009]; valence and arousal from FAN [@monnierAffectiveNormsFrench2014] and, for children and adolescents, FANchild [@monnierAffectiveNorms720French2017]; LIWC French dictionary. |
| Italian | Frequency, SUBTLEX-IT [@crepaldiSubtlexitSubtitlebased2015]; concreteness, imageability and age of acquisition [@dellarosaBeyondAbstractconcreteDichotomy2010]; valence, arousal and dominance [@montefineseAdaptationAffectiveNorms2014]; LIWC Italian dictionary. |
| Portuguese | Frequency, SUBTLEX-PT-BR [@tangCorpus61Million2012]; imageability, concreteness and subjective frequency from the Minho Word Pool [@soaresMinhoWordPool2017]; valence, arousal and dominance [@soaresAdaptationAffectiveNorms2012]; LIWC (Brazilian) Portuguese dictionary. |
| Polish | Frequency, SUBTLEX-PL [@manderaSubtlexplSubtitlebased2015]; valence, arousal, concreteness, imageability and age of acquisition from ANPW_R [@imbirAffectiveNorms4900Polish2016]. |
| Chinese (Mandarin) | Frequency, SUBTLEX-CH [@caiSubtlexchChineseWord2010]; valence and arousal from CVAW [@yuBuildingChineseAffective2016]; LIWC Chinese dictionary. |
| Russian, Turkish | Frequency from `wordfreq`; age of acquisition from the cross-language set of @luniewskaRatingsAgeAcquisition2016 (both are among its 25 languages); valence and arousal from NRC VAD; a published LIWC translation dictionary; concreteness and imageability treated as missing. |
| Romanian | Frequency from `wordfreq`; valence and arousal from NRC VAD; a published LIWC translation dictionary; age of acquisition (Romanian is not among the 25 languages of @luniewskaRatingsAgeAcquisition2016), concreteness and imageability treated as missing. |
| Hebrew (and Greek, if present in the corpus) | Frequency from `wordfreq`; age of acquisition from the cross-language set of @luniewskaRatingsAgeAcquisition2016; valence and arousal from NRC VAD; concreteness, imageability and LIWC categories treated as missing. |
| Korean, Indonesian, Arabic | Frequency from `wordfreq`; valence and arousal from NRC VAD; age of acquisition and concreteness generally unavailable and treated as missing. |
| Thai | Frequency treated as missing (`wordfreq` does not cover Thai); valence and arousal from NRC VAD; age of acquisition and concreteness treated as missing. The dependency-based syntactic features are verified before use, and the language is excluded from the affected analyses if automatic word segmentation proves inadequate (see Materials). |

: Validated Native Norm Sources by Language for the Lexicon-Dependent Measures {#tbl-norms tbl-colwidths="[20,80]"}

*Note.* Entries not listed use the cross-language defaults described in the text (`wordfreq` for frequency, the NRC VAD lexicon for valence and arousal, and the @luniewskaRatingsAgeAcquisition2016 set for age of acquisition) or are treated as missing under the pre-screening rule. Native frequency norms additionally validate the `wordfreq` estimates. AoA = age of acquisition; NRC VAD = NRC valence–arousal–dominance lexicon; LIWC = Linguistic Inquiry and Word Count.

```{=latex}
\endgroup
```

The exact set of generation languages, and hence the rows of this appendix, will be fixed from the platform metadata at data extraction (see Participants).

{{< pagebreak >}}

# References
